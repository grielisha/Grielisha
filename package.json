{
  "name": "grielisha-ecosystem",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-frontend": "cd frontend && npm install",
    "build-frontend": "cd frontend && npm run build",
    "install": "npm run install-frontend",
    "build": "npm run build-frontend && mv frontend/dist ./dist"
  }
}
